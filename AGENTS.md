# 说画项目架构约定

以下约定约束本项目的应用设计与实现；Jev-only 指应用运行时的自然语言理解，不改变开发代理的工具使用规则。

## 组合能力，不穷举对象与操作的组合

- 按“操作 × 目标对象／参照对象角色 × 属性 × 参数”建立统一命令协议。新增图形或对象类型时，注册其定义、语义候选、能力及必要的 tldraw 适配；已有移动、缩放、填充、复制、删除、连线等操作通过能力声明复用，不为每种对象重新编写解析和执行流程。
- 新增操作或属性也应通过统一协议与能力接口扩展。对象的底层差异留在适配器；不支持的组合统一拒绝。新增一种候选不代表支持任意操作，禁止静默近似替代。创建请求中的“和某对象一样”是属性约束，不能只执行默认创建、丢掉约束；实现前须澄清或明确拒绝。
- 跨类型参照创建已支持明确的“和某对象一样大／同宽／同高”：Jev 分开判断新对象类型、方位参照、尺寸参照及尺寸范围，执行层从实时画布读取外接宽高并校验后创建。继续扩展样式等属性时沿用这些独立角色，不为“弄／放／做”等动词添加专用解析分支。复制现有对象仍是同类型复制；泛称“一样”涉及哪些属性若未定义清楚就先澄清，不擅自继承颜色、文字、连接等状态。用换说法、换类型、不同尺寸与样式，以及歧义参照验证。
- 修改已有对象的参照编辑要把**被修改目标、提供值的参照对象、目标属性、来源属性**作为独立角色，不把整句里的所有序号都当成目标，也不按“开始／第二个”等具体说法写分支。当前可参照宽度、高度、整体颜色、填充、描边和文字颜色；宽度与高度可以交叉参照，其他属性只支持同属性参照。执行时从当前页面读取真实尺寸或样式，同一对象、缺失对象及不兼容类型要拒绝，未说清具体属性的“一样”要澄清。新增可参照属性时沿用同一命令协议和属性适配，不复制未明确要求的其他属性。

## 当前重构进度与扩展路径

- 已增加独立的 Jev 语义帧 `editFrame`：按画布变化判断 `create`（新增对象）、`recolor`（现有对象改色）、`connect`（现有对象间连线）；`colorRole` 另判断整体颜色、填充或描边。这些是对原有 `operation`、`operand`、目标和属性判断的语义补充，不是对所有操作的统一替代。现阶段只有这三类变化接入语义帧；移动、缩放、文字、复制等仍沿用现有命令合成路径。
- 对已接入的变化，先验证语义帧与旧操作／操作对象是否一致；相互冲突时澄清，不让旧的写字／新建兜底把它改写成另一种操作。旧的近指历史对象放行只保留在删除和移动路径。迁移其他操作时，先定义与画布变化相对应的语义角色及冲突边界，再接入统一命令；逐步收窄旧兜底，不靠注释整块旧 Jev 逻辑或新增逐句补丁来完成迁移。
- 新增语义帧也必须继续过否定／无关门控、真实对象解析、参数校验和执行前状态检查；Jev 只判语义，不选择不存在的对象、不生成精确值。每迁移一类操作，验证它与未迁移路径的交界、冲突时画布不变，并用换说法与不同画布状态检查复用。
- 第一阶段收敛已移除重复的 `attributeCategory` 和 `creationConstraintScope` 判断：普通属性由 `attributeDetail` 判定，新建时要求继承的属性由 `creationSizeIntent` 判定；已迁移的改色帧仍有 `colorRole` 对低置信属性细项的兜底，后续要继续收敛。创建尺寸来源、参照编辑的目标和来源、连线两端共用候选角色解析入口；返回的 ID 必须属于当前画布候选。继续迁移时优先让一个语义事实只有一处判断，并复用角色解析，不增加与已有字段同义的新题。
- 澄清状态下优先把本次话语独立解释为新指令；不能独立成立且 Jev 判断为补充时，才让原请求提供已有的操作、目标和属性，新话语只提供缺失字段。数字补充的精确值由代码从本次原文校验，不能让原请求里未决的多个数字继续污染新值；未决问题与完整新指令必须各自验收，禁止混合两句后静默执行另一项操作。

## Jev 理解语义，代码提供事实并执行

- 操作意图、对象类别、属性含义、目标指代及语音噪声的语义容错统一由 Jev 判断。不给自然语言设置本地关键词／正则意图解析、句式专用分支或无密钥回退；也不通过不断添加提示词特例来代替通用定义。缺少密钥、请求失败或含义不明确时，不修改画布并说明原因。
- 提供判断所需的最小真实画布上下文与候选，包括相关文字、选区、空间／连接关系及最近操作。Jev 返回受约束的类型化判断；代码负责原文截取、精确数字与单位解析、候选解析、坐标计算、能力及有效性校验、tldraw 写入、队列与撤销，不让模型编造节点 ID 或计算结果。
- 明确的语言目标约束优先于不相关选区；省略指代才结合选区和会话解析。目标缺失、冲突或多个候选无法区分时澄清，禁止默认改动当前选中对象。执行前校验实际目标符合命令约束，并检查是否允许执行；否定、无关和歧义结果不得落笔。

## 修复通用规则，用变化场景验收

- 修复前定位错误所属层及违反的通用规则：转写、上下文、Jev 判断、命令合成、目标解析或执行。修复一类问题，不为用户刚提供的句子新增专用规则；失败原句进入回归测试。
- 除失败原句外，按本次改动选择少量有代表性的换说法、换对象类型、换选区或画布状态验证复用，不穷举所有组合与边缘表达。核心回归覆盖 Jev 唯一执行路径、明确目标优先、歧义／否定不改画布，以及受影响的文字保护、监听、取消和撤销；语义理解效果必须区分真实 Jev 验证与模拟返回测试。
- 核心回归通过、已发现的范围内关键问题解决后及时交付，不自动扩展测试范围。修复后仅复测问题及受影响行为，复用仍有效的检查结果；其他边缘情况明确记录为未验证或已知限制，不作为无限延后交付的理由，也不能把已知错误执行归为边缘情况跳过。
