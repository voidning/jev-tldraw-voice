/**
 * Jev 语义判断题库。
 *
 * 解释器一次并行询问下面全部判断，每个 key 是一道独立的受约束选择题：
 * instructions 是给模型的判断依据，criteria 是允许返回的枚举值。
 * 所有判断只回答语义，不选择画布对象、不计算数值。
 *
 * 排版约定（不影响运行时）：较长的 instructions 按中文标点折行后用 join('') 拼回，
 * 拼接结果与原字符串逐字符相同；criteria 超过一行宽度时每个选项各占一行。
 */

const policy=[
  '根据原始 utterance 整句和 canvas 上下文判断本问题。operationText 仅把引号内内容遮蔽，',
  'quotedContent 保存原字；引号内容不能当动作/颜色/数字参数，允许有充分语境支持的同音/近音、漏字、',
  '错误断句；无足够证据选未知。各问题独立并行。',
].join('');

const endpointPolicy=[
  '连线端点按语言中的方向关系区分起点与终点；未表达方向的对称连接，',
  '按原文提及的先后顺序把第一个不同对象作为起点、第二个作为终点。',
  '对象的空间位置与当前选区不能改变此顺序。',
].join('');

/** 通用 policy 会拼在每题 instructions 之前；decision 与 valueIntegrity 例外，各自整段给出。 */
const choice=(instructions,criteria)=>({type:'choice',instructions:policy+instructions,criteria});

/** 已重构：从“画布发生了什么变化”出发，把变化类型、对象部位、创建时的
 * 属性约束拆成彼此独立的语义判断。命令合成层再结合真实画布中的目标和参照对象，
 * 校验这些答案能否同时成立；不能把某个动词或一句固定话术直接映射成命令。
 * 这里只表示这些判断已接入新路径，不表示整条创建／改色／连线链路已摆脱旧判断。 */
const refactoredQuestions={
  // 画布要发生哪一种变化：新增对象 / 已有对象改色 / 两对象之间连线
  editFrame: choice(
    [
      '只判断整句是否在肯定地要求以下一种画布变化，不检查对象是否唯一、位置或颜色值是否齐全。',
      '按语义和句中对象的角色判断，不按某个动词的字面写法判断：新对象出现是 create；',
      '已有对象的颜色发生变化是 recolor；两个已有对象之间出现一条连接或箭头是 connect。',
      '没有明确肯定请求时选 none，尤其是否定、条件假设、询问效果、聊天或只描述现状。',
      '若一句话同时要求多种变化或无法判定选 unknown。',
      '不要把“画一条从 A 到 B 的线”当成创建独立几何图形：A 和 B 是连接端点。',
    ].join(''),
    {
      create: '肯定请求新增一个图形对象',
      recolor: '肯定请求改变已有对象的颜色',
      connect: '肯定请求在两个已有对象之间建立连接',
      none: '没有肯定的画布变化请求',
      unknown: '请求种类不明确或包含多个变化',
    },
  ),

  // 改色作用在哪个部位：整体 / 内部填充 / 边框
  colorRole: choice(
    [
      '只检查原句是否明确点名颜色变化作用的对象部位，不判断动作、颜色值、目标或执行意愿。',
      '动词描述上色的方式，不等于明确点名部位；只有内部、填充区域等部位名词才是 fill，边框、轮廓、',
      '描边等部位名词才是 stroke。没有点名部位时选 whole；完全没有颜色变化选 none。',
    ].join(''),
    {
      whole: '颜色变化但没有明确点名内部或边框部位',
      fill: '明确点名内部或填充区域',
      stroke: '明确点名边框、轮廓或描边',
      none: '没有颜色变化',
      unknown: '明确提到了部位但无法判断是哪一部分',
    },
  ),

  // 新建对象要求继承哪些属性：目前只执行尺寸约束，其余明确拒绝。
  creationSizeIntent: choice(
    [
      '只判断用户在创建新图形时，是否要求新图形继承已有对象的尺寸；不判断落点、图形类型、参照对象是谁。',
      '明确要求与已有对象一样大、同样大小或宽高一致时选 sameBounds；',
      '仅要求同宽或同高时选 width 或 height；没有尺寸继承要求选 none。',
      '只说“做一个一样的”而没有说清“一样”指尺寸、颜色还是其他属性时选 ambiguous；明确要求复制颜色、',
      '样式、文字等非尺寸属性时选 unsupported，即使同时要求尺寸也必须选 unsupported，不能只做尺寸而丢弃其他约束。',
      '不要因句中提到已有对象作落点参照就推断有尺寸要求。',
    ].join(''),
    {
      sameBounds: '新对象的宽和高都要与已有对象的当前外接尺寸相同',
      width: '只让新对象与已有对象同宽',
      height: '只让新对象与已有对象同高',
      none: '没有要求继承已有对象的尺寸或其他属性',
      ambiguous: '要求“一样”但没有明确是哪些属性',
      unsupported: '要求继承颜色、样式、文字或其他当前不支持的属性',
    },
  ),

  // 属性由 attributeDetail 判断；本题只判断值的来源，避免再问一遍属性名。
  referenceIntent: choice(
    '只判断修改已有对象的属性值是否来自另一个已有对象。明确要求采用另一个对象的属性值时选 yes，来源属性可以与目标属性不同；直接给数字、颜色或相对档位时选 no。只说“和它一样”却没有明确哪项属性时选 unknown；没有属性编辑选 none。不判断目标、参照对象或属性种类，也不估算值。',
    {yes:'属性值来自另一个已有对象',no:'属性值由用户直接给出或使用默认档位',none:'本句没有修改已有对象的属性',unknown:'提到了属性参照但没说清具体属性'},
  ),

  // 被修改属性与提供值的属性是两个语义角色，可以不同。
  referenceSourceProperty: choice(
    '仅当把已有对象的属性设成另一个对象提供的值时，判断提供值的对象被要求提供哪个属性。若只说与它一样、没有单独指定来源属性，选 same，表示沿用被修改的属性；若明确点名来源的宽度、高度或颜色部位，选对应项。不要把“被修改对象的属性”误当成本题答案。不涉及参照编辑选 none；来源属性无法判定选 unknown。',
    {same:'没有单独指定来源属性，使用被修改对象的同一属性',width:'来源对象的宽度',height:'来源对象的高度',color:'来源对象的整体颜色',fill:'来源对象的填充',stroke:'来源对象的描边',textColor:'来源对象的文字颜色',none:'没有参照编辑',unknown:'无法判断来源属性'},
  ),
};

/** 待重构：旧路径先把整句分别归入 operation、operand、target、属性、值等
 * 命令字段，再由合成层按操作分支解释和拼装。字段之间有语义重叠，遇到新说法
 * 容易出现判断冲突或丢失约束；后续应逐类改为上面的“变化 × 对象角色 × 属性
 * 约束”判断，并逐步收窄旧合成分支。decision、valueIntegrity 等通用门控和
 * viewIntent 等独立能力也暂列在这里：它们目前仍参与旧路径，不能直接删除。 */
const pendingQuestions={
  // ── 总门控与输入校验 ──
  // 不带通用 policy 前缀：整段 instructions 由本题自己给出。
  // 总门控：本句是要编辑画布、要回退上一步、是否定，还是与画布无关
  decision: {
    type: 'choice',
    instructions: [
      '只判断 utterance 与画布的关系：是肯定的编辑请求、要求画布回退、否定，还是与画布无关。',
      '不在此问题检查目标、文字、数字、单位、对象是否齐全（其他问题负责）：只说一个动词、',
      '省略宾语或没提对象都不使本题变成含糊。允许结合整句支持的语音同音/近音、漏字、错误断句理解动作；',
      '不能凭可执行性把聊天强行当作请求。引号内的字是待写内容，不是否定操作。',
      '回退与否定按“画布要不要回到上一步之前”区分：要求把已经生效的那一步操作收回、',
      '让画布回到它之前的状态才选 revert；阻止一个尚未执行的动作、放弃自己正要说的请求，',
      '画布不需要回退，一律选 negated。画布上刚有别的操作不使否定变成回退。',
      'canvas.lastEdit 记录刚完成的那一步，为 null 时画布上还没有已完成的操作，此时只能选 negated。',
      '回退是回顾已完成的事实，不是修改它的对象或属性。句中带明确动作动词（删除、删掉、移走、改成红色、',
      '锁上）时，对象无论用“刚才建的”“刚画的那个”“上一个”还是代词指代，都只是那个动词的宾语，',
      '是对它的新的编辑请求，选 execute——revert 只出现在没有新动作、',
      '单纯要求收回已生效那一步的句子里，句中有动作动词就不属于此列。',
    ].join(''),
    criteria: {
      execute: [
        '肯定请求编辑画布或停止聆听，即使还有参数需由其他问题确认；只说一个明确的画布操作动词（重做、',
        '解锁、复制、删除等）而不提对象，仍然是编辑请求；',
        '带明确动作动词而对象用“刚才建的／刚画的那个”这类指代时同样是新请求，',
        '不因为句中出现“刚才”就变成回退',
      ].join(''),
      revert: [
        '要求把画布恢复到上一步操作之前，即收回、撤回刚刚完成的那一步。',
        '只说“撤销”“撤回”“撤回刚才那一步”“上一步不算”这类不带新动作、',
        '也不再描述任何对象的指令就属于此类：它指向的是已经生效的那一步，不是要阻止一个还没执行的动作。',
        'canvas.lastEdit 不为 null 时，这类指令就是回退。',
      ].join(''),
      action: '不能确定是否请求编辑或动作含义含糊',
      negated: [
        '阻止或否定尚未执行的动作，或放弃自己正要说的请求，不要求画布回退；',
        '描述某个对象应当保持的状态（“别让它动”＝锁定它）是在要求编辑，不是否定。',
        '“撤销”“撤回”这类不带宾语、也不指向任何对象和动作的指令不是在放弃请求，它们属于 revert；',
        '只有明确指向某个还没做过的动作才是否定，例如“不要画了”“别删”“不用加了”。',
      ].join(''),
      unrelated: '聊天、条件假设、询问效果，或没有请求画布编辑',
    },
  },

  // 同样不带通用 policy 前缀：只检查本句实际提供的字面值。
  // 本句实际给出的文字、精确数字、单位有没有歧义
  valueIntegrity: {
    type: 'choice',
    instructions: [
      '只判断本句实际提供的文字、精确数字或单位有没有歧义。不要检查对象是否选中或操作是否合法。',
      '“一点/再大点”是相对档位，没有精确数字，因此 clear。没有文字或精确数字时 clear。',
      '把文字放进对象或写到画布上的各种说法（写／写上／写入／写个／放／放上／放个／放入／填／填上／填个',
      '／加／加上／添／标／标注／记／输入／打成／改成／换成／写成）里，',
      '紧跟动词的“一个／个／一句／两个字／一行／一段／几个字”只是文字的量词，',
      '不是本句要提供的数值或数量，直接 clear——不要说它数量不明；',
      '只有像“复制两个”“画三个圆”这种真的在说对象个数的才另论。',
      '序号引用里的数字是创建顺序而不是数值：',
      '“第二个”“第一和第三个”“前三个”“后两个”“最后两个”“最后一个”中的数字一律不属于本句要提',
      '供的数值，直接 clear，不要追问单位或数量。',
      'quotedContent 中已经机械提取的引号内容是用户明确引用的文字，对其直接选 clear，不评价正确拼写，',
      '不把其中数字当尺寸。引号里明确要求原样写入的字（即使像错别字、否定句、数字）是内容，不要纠正，',
      '选 clear；用户表达两个备选、边界不清或要求恢复听错的文字时才选 text。精确长度要明确像素单位；',
      '数量和序号不需要像素单位。',
    ].join(''),
    criteria: {
      clear: '没有实质文字/数字/单位歧义',
      text: '要显示的文字或文字边界有歧义',
      number: '精确数值、数量、序号有歧义',
      unit: '精确长度缺失单位、单位有歧义或不支持',
    },
  },

  // ── 镜头取景 ──
  // 是否要求改变镜头取景范围（看全部 / 只看选中）
  viewIntent: choice(
    [
      '只判断本句要不要改变镜头能看到画布上的哪一部分，不判断任何对象、属性或尺寸。取景范围指相机取景：',
      '要求把整个画布的内容都纳入视野（看全部、全览、看整体、看全貌、看整个画布）选 fit；',
      '要求只把当前选中的对象纳入视野（聚焦到选中的、只看选中的那个）选 selection。',
      '没有要求改变取景范围一律 none——包括只是说话时提到“看看”“看一下”却没有说明要看全部还是看选',
      '中的。改变某个对象大小的说法（放大、缩小、大一点、高一点）属于 adjust，不是本题。',
    ].join(''),
    {
      none: '本句没有要求改变镜头能看到画布上的哪一部分',
      fit: '要求把整个画布的内容都纳入视野',
      selection: '要求只把当前选中的对象纳入视野',
      unknown: '提到镜头或取景，但不能确定要看全部还是只看选中的',
    },
  ),

  // ── 操作 ──
  // 操作维度：新建 / 写字 / 改值 / 相对调整 / 连线 / 复制 / 移动 / 排序 / 锁定 / 撤销重做等
  operation: choice(
    [
      '只判断操作维度，不判断对象/参数是否明确。用户请求写入/替换具体文字内容一律 write，',
      '独立文字或标签由位置区分；文字颜色仍是 set。“建/画/创建一个图形”是 add。“改成红色”是 set。',
      '把尺寸或颜色定成一个确定的值是 set——包括“调成和某对象一样宽／高”这种把值说成另一个对象的尺寸。',
      '“放大/缩小/宽一点/高一点”是 adjust；对尺寸增减不是 move。形状名称不代表新建。',
      '改变多个对象之间的对齐或前后遮挡顺序是 order：让它们靠左／靠右／顶对齐／居中对齐，',
      '或把它们放到最前面／最后面／上移一层／下移一层。',
      '“移到某个对象的上面／下面／左边／右边”说的是位置而不是层叠顺序——层叠的说法是不指名另一个对象',
      '的位置变化（“放到最前面”“上移一层”），凡是指着另一个对象说往哪边走就是 move。把对象锁上、',
      '让它不能被移动或修改是 lock，解除是 unlock。重做刚才被撤销的操作是 redo。',
    ].join(''),
    {
      add: '新建几何图形或添加描边，不包括写字',
      write: '在画布上写字或在已有图形内添加/替换文字内容',
      delete: '删除对象、内部文字、填充或描边',
      set: '把尺寸或颜色定成一个确定的值，包括把值说成另一个对象的尺寸（“和某对象一样宽／高”）；不包括写字',
      adjust: '相对调整尺寸',
      connect: '连接两个现有对象',
      duplicate: '复制对象',
      move: '移动对象',
      arrange: '横向或纵向把对象排开，不改变它们之间的对齐与遮挡',
      order: '改变多个对象之间的对齐或前后遮挡顺序',
      lock: '锁定对象，使其不能被移动或修改',
      unlock: '解除对象的锁定',
      undo: '撤销上一步',
      redo: '重做刚才被撤销的操作',
      stopListening: '停止麦克风聆听',
      unknown: '没有明确受支持操作',
    },
  ),

  // 撤销时是否限定了操作类型
  undoAction: choice(
    [
      '仅当撤销操作：用户是否明确限定要撤销哪一类操作？仅泛指上一步或刚才返回 none；',
      '明确提到连线/移动/复制等必须保留操作约束，不从 canvas.lastEdit 猜用户意图。',
      '若无法以受支持操作表达限定则 unknown。',
    ].join(''),
    {
      none: '未限定操作类型',
      add: '创建对象',
      set: '设置属性或文字',
      adjust: '相对尺寸修改',
      move: '移动',
      delete: '删除',
      duplicate: '复制',
      arrange: '排列',
      order: '对齐或前后层叠',
      connect: '连线',
      unknown: '限定无法确定或无法支持',
    },
  ),

  // ── 连线端点 ──
  // 连线的端点是怎么被引用的（成对指代 / 这个到那个 / 明确描述）
  connection: choice(
    [
      '假设本句要连线，仅判断端点引用形式，不检查是否恰好选中两个（执行器检查）。',
      '“它俩/这两个/他两”是一对，选 pair；“这个连到那个”是 pointed。出现明确端点类型、',
      '标签或位置则 explicit，不能选 pair/pointed。',
    ].join(''),
    {
      pair: '双选、最近对象对或页面唯一两个对象',
      pointed: '选中起点和鼠标悬停终点',
      explicit: '按明确类型/标签/空间描述指定端点，必须从候选选择，不能退回选区',
      unknown: '端点不明确',
    },
  ),

  // 连线起点在原句里写明的图形类型
  fromType: choice(
    endpointPolicy + '只读取起点在原句中明确写出的类型；不要读取终点的类型，也不要从画布推测。',
    {
      circle: '圆形',
      rectangle: '矩形',
      diamond: '菱形',
      text: '独立文字',
      none: '未明确类型',
      unknown: '类型不明',
    },
  ),

  // 连线终点在原句里写明的图形类型
  toType: choice(
    endpointPolicy + '只读取终点在原句中明确写出的类型；不要读取起点的类型，也不要从画布推测。',
    {
      circle: '圆形',
      rectangle: '矩形',
      diamond: '菱形',
      text: '独立文字',
      none: '未明确类型',
      unknown: '类型不明',
    },
  ),

  // ── 目标对象 ──
  // 操作的直接语义槽位：整个对象 / 内部文字 / 属性值
  operand: choice(
    [
      '判断操作的直接语义槽位。独立地在鼠标处“写X”是新建文字对象 object；',
      '已有对象内“写X/放一个X”是 text。文字颜色/字体尺寸是 property。改变整个形状的大小、宽高、',
      '颜色也属于 property，不属于 object。只有创建/删除整个形状、移动、复制、排列属于 object。',
      '在圆形中间放一个开始是 text，不是创建圆。“在某个对象上面／下面／左边／右边画、建、',
      '放一个图形名”是给新建图形定位置——方位词说的是画布上的位置，不是把东西放进那个对象里，',
      '一律 object，不因为“上面”也能读作“在……表面上”而改判 text；',
      '“在第二个上面画一个圆”就是新建一个圆放在第二个上方。',
    ].join(''),
    {
      object: '创建、删除整个对象，或复制、移动、排列；不包括改大小或颜色',
      text: '在已有图形内添加、替换或删除标签文字；不包括文字颜色',
      property: '对象的尺寸、填充色、描边或文字颜色',
      none: '不明确',
    },
  ),

  // 涉及的对象类别：几何图形还是独立文字
  objectFamily: choice(
    '仅判断所涉及对象类别。',
    { graphic: '圆形、矩形或菱形', text: '独立文字对象', unknown: '未明确或不支持的类别' },
  ),

  // 要新建的那个图形的种类
  shapeKind: choice(
    [
      '只判断本句要新建的那个图形的种类，绝不判断任何已有对象、也绝不判断用来定位的参照物。',
      '新建对象由“建／画／放／加一个某图形”说出，那个图形名就是答案。',
      '给新对象定位的另一个对象不是本题的对象：“在方形下面建一个圆”里的“方形”、',
      '“在第二个下面画一个圆”里的“第二个”都只是落点参照，它们的形状与本题无关。',
      '**参照物用序号或代词指代时（第二个、第三个、它、那里），',
      '不要到 canvas 里查出那个位置上的对象是什么形状**，',
      '更不要因为画布上恰好有这种形状就跟着选它——照着画布挑出来的形状不是用户说出的形状。',
      '句中确实没说出新建图形的种类（“在这里建一个”“再画一个”）才选 none。方形、方框、正方形、',
      '长方形属于矩形；可以基于整句容错但不可仅凭页面存在该形状推断。',
    ].join(''),
    { circle: '圆形', rectangle: '矩形', diamond: '菱形', none: '形状不明确或不支持' },
  ),

  // 原句对已有目标明确给出的类型约束
  explicitType: choice(
    [
      '仅判断本句明确约束的已有目标类型。必须保留用户明确说出的类型（包括同音容错后的类型），',
      '不能因选区是别的类型而忽略。圆/矩形/菱形指目标时返回该类型；文字颜色中的文字不是对象类型。',
      '凡原句没有具体几何种类/独立文字类型就 none，包括代词、泛称对象/图形、仅按标签或空间描述；',
      '不能从候选外观或位置补出用户未说的类型。新建对象的类型由 shapeKind 判断，',
      '这里只判断已有引用对象：句子用的是“建、画、放、加一个某图形”这类新建说法时，',
      '那个图形名描述的是即将新建的对象，不是已有目标，本题必须选 none，',
      '不得把新对象的类型写成对已有目标的约束。只有当句子在说“把／把那个／已有的某图形”时，',
      '图形名才是已有目标的类型约束。泛称（图形、形状、框、对象）不是具体几何种类，也一律 none。',
      '只用来描述另一个对象所在位置的图形名（“在方形下面建一个圆”“把第一个移到方形右边”里的“方形”',
      '）是在给参照物定位，不是对目标的类型约束，本题选 none。但被指定为文字接收容器的对象不是参照物：',
      '“在方形中间写一个结束”“在菱形里边写上开始”里的“方形／菱形”就是本句的目标本身，',
      '它的图形名是对目标的类型约束，本题返回那个类型。',
    ].join(''),
    {
      circle: '明确指向圆形',
      rectangle: '明确指向方形/矩形',
      diamond: '明确指向菱形',
      text: '明确指向独立文字对象',
      none: '没有显式目标类型约束',
      unknown: '目标类型含糊',
    },
  ),

  // 目标是怎么被引用的：序号 / 顺位 / 标签 / 空间关系 / 整组 / 全部某类
  target: choice(
    [
      '只分类 utterance 中实际说出的引用限定，不在此题选择画布对象。对象是否唯一、',
      '是否处于第一个位置不影响本题，不能从 canvas 推出用户没说的序号。类型由 explicitType 单独判断。',
      '没有额外限定就是 default。各选项按语言证据互斥：说出序号或序号范围（第二个、第一和第三个、',
      '前三个、后几个）一律用 sequence，具体数值由代码从原句读取，本题不判断数值，',
      '也不因为没有量词而改判；',
      '句中出现两个序号而其中一个只是给参照物定方位（“把第一个移到第二个右边”里的“第二个”）时，',
      '本题仍按目标实际使用的序号判为 sequence，数值由代码剔除参照占用的那一个；不带具体序号、',
      '仅按创建顺序两端指代单个对象才用 last／middle；按已有标签、',
      '或按与某个对象的空间关系指代一个已有对象用 object；仅上一操作产生的对象用 previous；',
      '明确整个组或复数指代（它们、这些、这几个、全部）用 all，',
      '当“全部／所有／每一个”后面还跟着明确图形种类（全部方形、所有圆形、每一个菱形）时改用 kindAll；',
      '只出现单数代词“它”或完全没说对象才是 default。',
      '宾语是整块画布本身（“清空画布”“清空所有内容”“把画布清空”“清空”）而句中没有序号或图形种类',
      '限定时，说的是删除全部对象，选 all，不能因为没有量词就当成 default。',
      '对象内部的文字位置不是对象引用限定；请求写入的新文字也不是用来查找已有对象的标签。',
      '“在某个对象下面／右边建、画、放一个新对象”只是在说新对象要放在哪里，不是对已有目标的引用限定；',
      '“把某对象移到另一个对象旁边”里的那个对象同样只是给方位定参照，两者本题都按没有额外限定处理，',
      '由 placementReference 单独判断。object 只用于有已有标签被当作查找条件，',
      '或明确按与另一个对象的空间关系挑一个已有对象的情况，',
      '不能仅因指令有文字内容或提到位置就选 object。',
      '“刚才建的／刚建的那个／刚画的那个／刚才那个／刚做出来的”都指最近创建的那个对象，',
      '一律按创建顺序读作最后一个，选 last，不因为“刚才”而改判 previous；',
      '“上一个／上一步的结果”才指上一次操作产生的对象，选 previous。',
    ].join(''),
    {
      default: '没有额外引用限定，只说目标类型或单数代词“它”，或完全省略目标；也包括只是给新对象指定落点',
      previous: '明确引用上一次操作产生的结果对象',
      middle: '明确引用创建顺序正中间的对象',
      last: '明确引用创建顺序最后的对象',
      sequence: [
        '明确说出创建顺序上的序号或序号范围（第几个、第一和第三个、前几个、后几个、倒数第一个），',
        '数值交给代码读',
      ].join(''),
      all: '引用一组对象：它们、这些、这几个、全部，或明确指当前选区/会话组，且句中没限定图形种类',
      kindAll: [
        '“全部／所有／每一个”后面跟着明确的图形种类（全部方形、所有圆形、每一个菱形），',
        '只要这一类图形的整体',
      ].join(''),
      object: '用已有标签、或与另一个对象的空间关系指代一个已有对象',
      unknown: '存在引用限定但无法确定其含义',
    },
  ),

  // 用「连到／从…连出」限定目标时的箭头方向
  relationKind: choice(
    [
      '本题只判断目标对象相对句中另一个对象的连接方向，',
      '不选择任何对象（那个对象由 relationAnchor 单独判断）。',
      '凡是用“连接到／连到／指向某对象的”这类连接关系限定目标的句子：',
      '箭头从目标出发指向那个对象时选 connectedTo；箭头从那个对象出发指向目标时选 connectedFrom；',
      '只提到有连接关系、未表达方向时选 any。没有用连接关系限定目标一律 none。',
      '方向只由这句话的语言决定：',
      '“连到某对象的”“指向某对象的”“箭头通向某对象的”描述的是目标发出的箭头，选 connectedTo；',
      '“从某对象连出去的”“某对象连接到的”描述的是从那个对象发出的箭头，选 connectedFrom。',
      '不要因为两个对象都可能符合、或画布上存在反向的连线就退回 any 或 unknown。',
    ].join(''),
    {
      none: '本句没有用连接关系限定目标',
      connectedTo: '目标发出的箭头指向那个对象',
      connectedFrom: '那个对象发出的箭头指向目标',
      any: '只提到有连接关系，未表达方向',
      unknown: '提到连接关系但方向无法确定',
    },
  ),

  // 对齐还是层叠，以及具体方向
  orderRelation: choice(
    [
      '本题只在 operation 判为 order 时判断是哪一种秩序关系，不选择对象（对象由目标题判断）。',
      '对齐指把多个对象的某一条边或中心线移到同一条线上，不改变对象大小，',
      '也不是把它们排成等距队列（那是 arrange）；层叠指改变对象之间谁挡住谁的前后顺序，不改变位置。',
      '左右按画布的水平方向，上下按垂直方向。没有提到对齐或前后层叠一律 none。',
    ].join(''),
    {
      none: '本句没有要求改变对象之间的对齐或前后层叠关系',
      alignLeft: '左边缘对齐（靠左对齐）',
      alignRight: '右边缘对齐（靠右对齐）',
      alignTop: '上边缘对齐（顶对齐）',
      alignBottom: '下边缘对齐（底对齐）',
      alignCenterH: '左右居中，各对象的垂直中轴线重合',
      alignCenterV: '上下居中，各对象的水平中轴线重合',
      layerFront: '移到最上层，不再被任何对象遮挡',
      layerBack: '移到最下层，被其他对象遮挡',
      layerForward: '向上移一层',
      layerBackward: '向下移一层',
      unknown: '提到秩序关系但方向不明确',
    },
  ),

  // ── 参照与落点 ──
  // 有没有指定某个已有对象作为文字容器
  writePlacement: choice(
    [
      '只判断本句有没有把一个已有对象指定为文字接收容器。画布位置指代与对象指代不同：',
      '鼠标落点/这里仅指定位置，不指定选中的对象。选区的存在不能补出用户未表达的文字容器。',
      '只请求在画布位置写字属于 canvas；明确写到已有对象内部或替换其文字才 label。',
      '内部位置说法（中间／中央／里面／里边／内部／之内／以内，',
      '以及“方形里”这种“里”）已经把那个已有对象指定成文字的接收容器，一律 label：',
      '“在方形中间写一个结束”“在菱形里边写上开始”都是把文字写进那个对象。',
      '文字量词（写一个／写两个字／写上一行）只是在说文字内容的多少，不是在说“新建一个对象”，',
      '不能因此判 canvas。canvas 要落在鼠标位置上（canvas.hasAnchor=true 才有落点），',
      'hasAnchor=false 时只有句中确实没有指定任何已有对象才选 canvas。文字内容里的数字、单位、',
      '标点不参与本题判断。',
    ].join(''),
    {
      canvas: '未指定已有对象作为文字容器，在画布创建独立文字',
      label: '明确指定已有对象接收文字或明确替换其已有文字',
    },
  ),

  // 句中方位词的含义（鼠标落点 / 内部 / 上下左右）
  position: choice(
    [
      '只判断句中位置/方向的含义，不要求用户说坐标。canvas.hasAnchor=true 表示已有鼠标落点，',
      '所以“这里/这儿/移到这里/在这里建”必须选 here，不能因为没说坐标选 none。',
      '方位词的同义说法都算说清了，不要退回 none：“上面/上方/顶部/上头”都是 above，',
      '“下面/下方/底部/底下”都是 below，“左边/左侧/左面”是 left，“右边/右侧/右面”是 right；',
      '方位词指向另一个对象时方位含义不变（“在方形下面建”仍是 below）。',
      '“内部/里面/中间写”选 insideCenter；与位置无关才选 none。hasAnchor=false 时没有鼠标落点，',
      '不能选 here。',
    ].join(''),
    {
      here: '鼠标标记位置',
      insideCenter: '对象内居中',
      inside: '对象内',
      left: '左侧',
      right: '右侧',
      above: '上方',
      below: '下方',
      none: '未指定',
    },
  ),

  // ── 属性与取值 ──
  // 具体属性：整体大小 / 宽 / 高 / 颜色 / 填充 / 描边 / 文字颜色
  attributeDetail: choice(
    [
      '具体哪个受支持属性？只有明确提到内部填充/涂满或描边时才选 fill/stroke；',
      '没有细分的整体改色是 color，不猜测填充或描边。整体放大/缩小同时改变两个轴才是 size；',
      '高/矮仅纵轴 height，宽/窄仅横轴 width，不能把高一点归为整体尺寸。文字颜色是 textColor，',
      '不是文字内容。',
    ].join(''),
    {
      size: '整体大小',
      width: '宽度',
      height: '高度',
      color: '整个图形的颜色，未特指填充或描边；保留填充模式',
      fill: '明确指定填充颜色或涂满内部',
      stroke: '描边颜色或有无描边',
      textColor: '文字颜色',
      none: '不明确或不支持',
    },
  ),

  // 尺寸是设为精确值还是相对增减
  change: choice(
    '尺寸变化方式是什么？不能把否定放大解释为缩小。',
    { set: '设为精确值', increase: '增大', decrease: '减小', none: '不明确' },
  ),

  // 给出的值属于哪一类：预设颜色 / 像素长度 / 相对档位 / 原样文字
  valueKind: choice(
    [
      '值的类型是什么？精确数字不能降级为大一点。',
      '仅“大一点/再大点/放大一点/宽一点”等默认相对档位应选 step，不需要数字或像素单位。',
    ].join(''),
    {
      literalColor: '预设颜色',
      length: '明确像素长度',
      step: '未给精确数值的相对一点',
      text: '要原样显示的文字',
      none: '没有值',
    },
  ),

  // 颜色词对应的预设颜色
  literalColor: choice(
    '假设本句是在设置颜色，选择表达的颜色；颜色操作语义可容错，但写到画布的颜色词不可改写。',
    {
      red: '红色',
      blue: '蓝色',
      gray: '灰色',
      green: '绿色',
      yellow: '黄色',
      orange: '橙色',
      violet: '紫色',
      black: '黑色',
      white: '白色',
      none: '未知、不支持或多个候选',
    },
  ),

  // ── 排列与分句 ──
  // 排列方向：横向还是纵向
  layout: choice(
    [
      '排列方向？复制时没说方向，应用默认横向，请选 horizontal；普通排列操作没说方向则 unknown。',
      '“排成一行／排成一排”说的是横向，“排成一列”说的是纵向，都算已经说明方向。',
    ].join(''),
    { horizontal: '横向一行，或复制时未指定方向的默认横向', vertical: '纵向一列', unknown: '不明确' },
  ),

  // 复制数量的含义：新增副本数还是最终总数
  quantityMode: choice(
    [
      '假设复制操作，数量含义是什么？“复制两个/再复制两个”是额外新增两个 additional；',
      '“复制成两个/总共两个”才是最终总数 total。不计算数值。',
    ].join(''),
    { additional: '新增副本数', total: '包含原对象的总数', unknown: '不明确' },
  ),

  // 多个分句是同一个操作的续说，还是彼此独立／有先后依赖的多个操作
  clauseRelation: choice(
    [
      '假设本句有标点或“然后”，判断是否包含多个独立操作动词和目标。一个操作被错误断成几段（对象、',
      '动作、程度各一段）仍是 continuation。例如“矩形，缩小，一点”只有缩小一个操作。',
      '文字引号内所有词和标点都不是操作。只有多个明确操作才选 separate/dependent。',
      '同一个动作落到序号不同或明确不同的多个目标上（“第一个改成蓝色，',
      '第二个改成红色”）是多个独立操作，选 separate，不因为动作词相同就算成一个操作。',
    ].join(''),
    {
      continuation: '一个操作，其他短语是补充或错误断句',
      separate: '同一个或不同的动作分别落到不同的目标上，各目标互不依赖',
      dependent: '多个有先后依赖的编辑步骤',
      unknown: '分句关系不明确',
    },
  ),
};

// 保持原有 question key 和顺序，解释器仍一次并行询问全部判断；分组只是迁移边界。
export const questions={...refactoredQuestions,...pendingQuestions};
