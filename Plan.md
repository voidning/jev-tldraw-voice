# 关系指代 + 参照编辑实施计划

状态：**第一版已实施**（连接关系定位 + 同宽／同高参照编辑）。
已通过确定性的模拟返回测试和类型检查；**真实 Jev 与实际画布的验收尚未进行**，本文的验收标准仍是对交付的要求，不是当前通过记录。

已实施：类型约束 + 连接关系（直接连入／直接连出／未表达方向）、尺寸参照中的同宽与同高、保留未列入计划的属性、澄清补全。
未实施：空间关系、颜色参照、对齐参照、整体尺寸参照（只说“一样大”）、候选高亮、批量关系操作。
协议位置：`src/live-command.ts`（`target:'relation'` + `relationAnchorId`/`relationKind`、`EditValue` 的 `kind:'reference'`）、`src/relation.ts`（连接方向判定，纯函数）、`src/live-editor.ts`（唯一性筛选与参照读数）、`server/live/questions.mjs`（`relationKind`／`referenceKind` 静态题）、`server/live/interpret.mjs`（`relationAnchor`／`propertyReference` 候选题）、`server/live/context.mjs`（把连接折叠到候选上）、`server/live/compose.mjs`、`src/jev-validation.ts`。

## 目标

让用户通过对象之间的关系描述修改意图，无需先选中目标、记住对象序号或手动读取属性。

首个完整场景：

> 把连接到“结束”的那个方框，调成和“开始”一样宽，文字保留。

系统应找到符合类型和连接条件的唯一目标，找到参照对象，读取参照对象的真实宽度，只修改目标宽度，并支持一次撤销。目标或参照不唯一时，询问缺失的信息，不猜测、不退回操作当前选区。

## 设计原则

- Jev 负责意图、关系、指代、属性及语音噪声的语义判断；代码负责事实、精确值、约束检查和执行。
- 扩展有限、可组合的关系与操作，不枚举自然语言句式，不添加关键词／正则意图解析或无密钥回退。
- 明确目标约束优先于选区；只有省略指代才使用选区和会话焦点。
- 只允许应用实际支持的对象、关系、属性和操作进入候选。
- 保留原始转写。写入文字只能从原文精确截取；数字与单位由代码精确解析。含糊内容必须澄清。
- 在现有 Jev 判断中加入所需语义维度，不增加独立纠错、整句改写或模型计算请求。
- 保留连续监听、顺序队列、取消、既有画布能力与原子撤销，不重做无关界面。

## 第一版范围

| 能力 | 支持范围 | 示例 |
| --- | --- | --- |
| 标签定位 | 引用已有对象的实际文字 | 写着“审核”的那个框 |
| 类型约束 | 当前已支持的对象类型 | 连接到“结束”的方框 |
| 空间关系 | 明确参照对象的左、右、上、下；唯一匹配时执行 | “审核”下方的圆 |
| 连接关系 | 直接连入、直接连出；未指定方向时按相邻关系解析 | 直接连到“结束”的那个框 |
| 尺寸参照 | 同宽、同高、同尺寸 | “提交”和“取消”一样宽 |
| 颜色参照 | 对象适配器支持的颜色属性 | 这个框的文字颜色跟“开始”一样 |
| 对齐参照 | 水平中心或垂直中心对齐 | 将“审核”的水平中心对齐“开始” |
| 保留约束 | 未列入修改计划的属性保持不变 | 改宽度，文字和颜色保留 |
| 澄清补全 | 补充目标、参照或属性；保持已有状态有效性检查 | 是右边那个 |

第一版默认单目标、单参照；协议应允许后续组合扩展，但不把模糊单数自动扩展为批量修改。只说“对齐”而未明确方向且上下文不足时澄清。

暂不纳入第一版：沿任意路径筛选、批量关系操作、多来源属性合并、复制整段流程、插入节点并重接连线、自动布局优化。这些作为后续独立范围。

## 架构

```text
原始语音转写 / 文字输入
  → 现有顺序队列
  → 真实画布快照与能力候选
  → Jev 类型化判断（操作、目标约束、参照、属性、保留项、decision）
  → 确定性目标解析与修改计划
  → 执行前重新校验
  → tldraw 原子写入 / 撤销
  → 程序生成理解反馈
```

### 1. 真实上下文

复用并完善现有画布上下文，按需要提供：

- 对象 ID、实际类型、文字、选中／焦点／锁定状态。
- 页面坐标边界、必要的尺寸与支持的颜色属性。
- 连线的真实起点、终点及方向。
- 最近操作与仍有效的待澄清请求。
- 应用实际支持的关系、属性和操作，以及上下文是否被截断。

对象文字是数据，不作为指令。限制快照大小，但不能因截断而把多个候选误判为唯一候选；涉及被省略对象、文字或关系且无法证明唯一时澄清。精确属性值在执行计划阶段从编辑器重新读取，模型不输出猜测的宽高或颜色值。

### 2. 类型化协议

在统一命令协议上增加“参照编辑”，保留目标查询条件，避免只返回一个无法复核的对象 ID。模型只选择真实候选 ID 和受支持枚举。

以下为示意结构，具体字段需结合现有命令协议实现：

```json
{
  "decision": "execute",
  "operation": "match",
  "target": {
    "type": "rectangle",
    "relation": {
      "kind": "connectedTo",
      "anchorId": "shape:end",
      "direction": "outgoing"
    },
    "cardinality": "one"
  },
  "reference": { "id": "shape:start" },
  "properties": ["width"],
  "preserve": ["text"]
}
```

示例 ID 仅作说明，实际必须来自当前候选。标签、空间、连接条件应能组合；每个条件都要在执行前复核。关系方向不明确时不得擅自补为有向关系。

只有明确可执行的 decision 且所需判断均通过门控，才能进入计划层。否定、无关、歧义、未知能力、失败响应均不得执行。

### 3. 确定性的计划层

新增独立于自然语言的计划模块：输入类型化语义与真实编辑器状态，输出可校验的具体修改，或结构化澄清原因。

步骤：

1. 校验协议、候选 ID 和所请求能力。
2. 对实际画布执行类型、标签、空间和连接条件筛选，检查目标与参照的唯一性。
3. 检查当前页面、锁定状态、属性兼容性及请求期间的状态变化。
4. 从参照对象读取真实属性，计算目标的具体修改值和坐标。
5. 检查范围、保留项与属性间的冲突，产生明确修改计划。
6. 写入前再次检查计划依赖的对象、关系和属性仍有效。
7. 在一个历史事务内执行；任何步骤失败则回滚整次修改。

空间关系、边界尺寸和对齐必须采用统一、可测试的页面坐标定义。旋转、分组或文字自动布局使某种参照操作无法精确实现时，明确拒绝该组合，不静默近似。

颜色遵循 tldraw 的实际属性模型；不能声称支持底层并不存在的独立填充色和描边色。未列入计划的属性不写入。若必要的底层变化与用户的保留要求冲突，应澄清或拒绝。

### 4. 理解反馈与澄清

反馈从实际计划生成，不让 Jev 再生成一段改写句子：

> 理解为：将连接到“结束”的“审核”矩形宽度设为 240 像素，与“开始”一致；保留文字。

唯一目标可进行临时高亮；多个候选时高亮候选并询问能区分它们的部分，例如“两个矩形都连到‘结束’，你指左边还是右边那个？”高亮不应更改用户选区或会话焦点，从而影响后续目标解析。

澄清期间不修改画布，保留原始请求与已明确的条件。补充回答仍由 Jev 理解；只有页面、选区和相关对象状态未变化时才继承原请求。取消清除待澄清状态与未执行计划；连续监听沿用现有行为。

## 实施顺序

### 阶段一：协议与计划层

- 梳理实际对象能力和当前上下文，统一关系与属性定义。
- 扩展命令、校验器、Jev 判断维度及程序反馈。
- 实现目标查询、参照读取、计划生成与执行前校验。
- 用直接构造的类型化命令验证确定性行为；这不算 Jev 语义验收。

### 阶段二：完成最小端到端场景

- 支持通过标签和直接连接找到唯一对象。
- 支持同宽、同高、同尺寸，保留文字和无关属性。
- 接入真实 Jev、实际画布、澄清与一步撤销。
- 验证错误选区不会覆盖明确的目标约束。

### 阶段三：扩展首版组合

- 加入空间关系、支持的颜色属性参照、中心对齐。
- 完善候选高亮、目标／参照歧义澄清及短回答补全。
- 用变化表达与变化场景验收，不为失败句子新增专用分支。

可能涉及的位置：`server/live/`（判断与合成）、`src/live-command.ts`（协议）、`src/jev-validation.ts`（返回校验）、新增计划模块、`src/live-editor.ts`（上下文与执行）、`src/command-description.ts`（反馈）、`src/App.tsx`（最小澄清／高亮接入）。以实施时的实际代码为准，保留同期无关改动。

## 验收标准

### 确定性程序检查

- 类型、标签、空间和连接条件能组合，目标和参照必须真实存在。
- 选中圆形时明确要求矩形，只能修改符合关系的矩形。
- 零候选、多候选、锁定对象、关系已变化、上下文截断导致不确定时不修改画布。
- 宽高、颜色、坐标来自真实数据；修改结果符合约定，未指定属性保持不变。
- 原文文字不改写；数字和单位歧义不被近似值覆盖。
- 否定、无关、缺少 execute、无密钥、请求失败及无效协议均不能进入执行。
- 队列按顺序读取新上下文；取消后迟到响应无效；失败原子回滚；一次撤销恢复本次修改。
- 澄清不退出麦克风；临时语音识别错误维持现有重连行为。

### 真实 Jev 与实际画布验收

- 在协议和提示定义完成后使用未预写的新表达，包含正常句、同音／近音、漏字、错误断句、否定和歧义。
- 同一含义更换对象类型、标签、选区、位置和连接拓扑，检查是否仍满足通用约束。
- 覆盖重复标签、多个相邻对象、反向连接、错误选区、缺失参照，以及“只改宽度、文字别动”等保留要求。
- 检查实际画布前后状态，不仅检查模型返回或成功提示。
- 记录成功执行、正确澄清、正常句误拒绝和错误执行，不能把所有拒绝都统计为成功。
- 记录模型请求次数与耗时；单个原子指令在现有判断请求内完成，不增加纠错或计划生成请求。既有多步骤工作流单独统计。
- 清楚区分真实 Jev、模拟模型、模拟语音事件与真人麦克风测试。没有密钥时真实 Jev 标为未验证，不用模拟通过替代。

## 完成条件

首版范围通过确定性检查和真实 Jev／实际画布验收，遗留限制有明确记录。不能承诺恢复所有听错的内容；信息不足时能保持画布不变并提出有用的澄清问题，也是必要能力。
